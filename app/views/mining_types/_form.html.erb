<%= form_with(model: mining_type, class: "needs-validation") do |form| %>
  <% if mining_type.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(mining_type.errors.count, "error") %> prohibited this mining type from being saved:</h2>
      <ul>
        <% mining_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :description, class: "form-label" %>
    <%= form.text_field :description, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :acronym, class: "form-label" %>
    <%= form.text_field :acronym, class: "form-control" %>
  </div>

  <div class="mt-3">
    <%= form.submit "Save Mining Type", class: "btn btn-primary" %>
  </div>
<% end %>
