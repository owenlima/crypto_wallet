<% content_for :title, "Editing coin" %>

<div class="container mt-4">
    <h1 class="text-primary">Editing Coin</h1>

    <div class="card p-4 shadow-sm mt-3">
        <%= render "form", coin: @coin %>
    </div>

    <div class="mt-4">
        <%= link_to "Show this coin", @coin, class: "btn btn-info" %> |
        <%= link_to "Back to coins", coins_path, class: "btn btn-secondary" %>
    </div>
</div>